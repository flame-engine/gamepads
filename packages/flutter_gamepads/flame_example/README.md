# flutter_gamepads flame_example

A simple flame game example project showcasing the flutter_gamepads plugin.

`flutter_gamepads` package is used to allow gamepad control over the overlays in the game.
While the spaceship is controlled with `gamepads` events directly.
