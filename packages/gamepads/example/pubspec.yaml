name: gamepads_example
description: A simple example project showcasing the gamepads plugin.
publish_to: 'none'

version: 0.1.0

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  gamepads: ^0.1.7

dev_dependencies:
  flame_lint: ^1.4.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
