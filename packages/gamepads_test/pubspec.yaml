name: gamepads_test
resolution: workspace
description: "A test package for gamepads package"
version: 0.1.0
homepage: https://github.com/flame-engine/gamepads
repository: https://github.com/flame-engine/gamepads/tree/main/packages/gamepads_test

flutter:

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  gamepads: ^0.1.11
  gamepads_platform_interface: ^0.1.3

dev_dependencies:
  flame_lint: ^1.4.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
